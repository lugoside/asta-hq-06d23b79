name: update-data

# Rigenera il listone (docs/data/players.json) dalla fonte ufficiale.
# - workflow_dispatch: avviabile A MANO, anche da telefono (GitHub app/web → Run workflow)
# - schedule: ogni mattina, utile nei giorni prima dell'asta
on:
  workflow_dispatch:
  schedule:
    # NB: gli orari cron di GitHub sono in UTC. A ago/set l'Italia è UTC+2 (CEST).
    # Fino al 31/08: una volta al mattino, ~08:00 IT (06:00 UTC), ogni giorno di agosto.
    - cron: "0 6 * 8 *"
    # 1–2 settembre: 06:00/10:00/14:00/18:00/20:00 IT -> 04/08/12/16/18 UTC
    - cron: "0 4,8,12,16,18 1,2 9 *"
    # 3 settembre (giorno d'asta): 06:00/10:00/14:00/18:00 IT -> 04/08/12/16 UTC (ultimo alle 18:00 IT)
    - cron: "0 4,8,12,16 3 9 *"

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Installa dipendenze
        run: pip install -r pipeline/requirements.txt
      - name: Scarica il listone
        run: python pipeline/fetch_sources.py
      - name: Ricostruisci players.json
        run: python pipeline/build_players.py
      - name: Commit e pubblica (robusto ai race)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          for i in 1 2 3 4 5; do
            git add docs/data
            if git diff --cached --quiet; then
              echo "Nessuna variazione nei dati."; exit 0
            fi
            git commit -m "chore: aggiorna dati listone [skip ci]"
            if git push; then
              echo "Pubblicato al tentativo $i."; exit 0
            fi
            echo "Push rifiutato (remoto avanzato): riallineo e rigenero (tentativo $i)…"
            git reset --hard HEAD~1
            git fetch origin main
            git reset --hard origin/main
            python pipeline/fetch_sources.py
            python pipeline/build_players.py
          done
          echo "Impossibile pubblicare dopo 5 tentativi."; exit 1
